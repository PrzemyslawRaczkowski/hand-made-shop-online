import React from 'react';

import MainComponent from '../components/MainComponent';

export default class Contacts extends React.Component {
    render() {
        return (
            <div>
                <MainComponent title="Contacts"/>
            </div>
        );
    }
}